# V2.3

Run `supabase-v2.3-archive-ratings-migration.sql` in Supabase SQL Editor, then replace `index.html`, `style.css`, and `app.js` on GitHub.

Archive now uses individual attendee ratings and displays the live average. The header always points to the strictly next Friday (on Friday itself, it points one week ahead).
