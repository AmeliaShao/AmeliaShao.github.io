# V2.6 hotfix

No Supabase migration is needed.

Fixes:
- Rating stars are permanently visible in the rating modal.
- `ARCHIVE THIS FILM` is permanently visible without hover.
- Hover/focus states remain as visual feedback only.

Upload/replace `index.html`, `style.css`, and `app.js` on GitHub Pages. `config.js` and Supabase do not need changes.
