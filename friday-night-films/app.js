const seed=[{title:'Perfect Days',year:2023,genre:'Drama',by:'Amelia',reason:'Quiet, beautiful, and exactly the Friday-night reset we need.',votes:3,poster:''},{title:'Chungking Express',year:1994,genre:'Drama · Romance',by:'Kevin',reason:'For the vibes. No further questions.',votes:2,poster:''},{title:'The Grand Budapest Hotel',year:2014,genre:'Comedy · Adventure',by:'Jess',reason:'Peak symmetry, tiny pastries, immaculate chaos.',votes:1,poster:''}];
let movies=JSON.parse(localStorage.getItem('fnf_movies')||'null')||seed;const grid=document.querySelector('#movies');
function save(){localStorage.setItem('fnf_movies',JSON.stringify(movies))}function render(){grid.innerHTML='';movies.sort((a,b)=>b.votes-a.votes).forEach((m,i)=>{let el=document.createElement('article');el.className='movie';el.innerHTML=`<div class="poster">${m.poster?`<img src="${m.poster}" alt="${m.title} poster">`:m.title}</div><h3>${m.title}</h3><div class="meta">${m.year||'—'} · ${m.genre||'Film'}</div><div class="by">Suggested by ${m.by}</div><p class="reason">“${m.reason}”</p><button class="vote" data-i="${i}">♡ ${m.votes} vote${m.votes===1?'':'s'}</button>`;grid.appendChild(el)});document.querySelectorAll('.vote').forEach(b=>b.onclick=()=>{movies[+b.dataset.i].votes++;save();render()})}render();
const now=new Date(),fri=new Date(now);fri.setDate(now.getDate()+((5-now.getDay()+7)%7));document.querySelector('#date').textContent=fri.toLocaleDateString('en-SG',{weekday:'long',day:'2-digit',month:'long'}).toUpperCase();
const modal=document.querySelector('#modal');document.querySelector('#open').onclick=()=>modal.showModal();document.querySelector('#close').onclick=()=>modal.close();document.querySelector('#form').onsubmit=e=>{e.preventDefault();let f=new FormData(e.target);movies.push({title:f.get('title'),year:'',genre:'Film',by:f.get('name'),reason:f.get('reason'),votes:0,poster:f.get('poster')});save();render();e.target.reset();modal.close()};
